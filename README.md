# TaskPinBot 📋

Простой Telegram-бот для управления задачами с автоматическим закреплённым сообщением статистики.

## Функциональность ✨

- **Автоматическая замена сообщений**: Бот удаляет сообщения пользователей и переотправляет их от своего имени
- **Интерактивные кнопки**: Создание и закрытие задач через inline-кнопки
- **Закреплённая статистика**: Автоматическое обновление закреплённого сообщения со статистикой задач
- **Кликабельные задачи**: Каждая задача в закреплённом сообщении - это ссылка на сообщение с кнопкой
- **Удобный формат**: Задачи отображаются столбиком в формате: `1. Текст задачи - @постановщик`
- **Поддержка нескольких чатов**: Каждый чат имеет свою базу задач и закреплённое сообщение
- **Сохранение в БД**: Все задачи сохраняются в локальной SQLite базе данных

## Установка 🚀

### 1. Клонирование репозитория
```bash
cd C:\git\task.pin.bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка токена
Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=ваш_токен_бота
```

**Токен уже настроен:** `7972958560:AAH81czyJC5JOHK1q6OD69DGSPV-fbg0Tmc`

### 4. Настройка бота в Telegram

**ВАЖНО!** Бот должен быть администратором чата с правами:
- ✅ Удаление сообщений
- ✅ Закрепление сообщений
- ✅ Отправка сообщений

## Миграция базы данных (если бот уже использовался) 📦

Если вы обновляете бота и у вас уже есть файл `tasks.db`, выполните миграцию:

```bash
python migrate_db.py
```

Это добавит необходимую колонку `message_id` в существующую базу данных.

## Запуск 🎯

```bash
python bot.py
```

При успешном запуске вы увидите:
```
==================================================
🚀 TaskPinBot запущен!
==================================================
📝 Функции:
  • Автоматическая замена сообщений пользователей на сообщения бота
  • Кнопки создания и закрытия задач
  • Автоматическое обновление закрепленного сообщения со статистикой
==================================================
```

## Использование 📝

### В Telegram чате:

1. **Добавьте бота в чат** и сделайте его администратором
2. **Напишите любое сообщение** в чат
3. Бот **удалит ваше сообщение** и отправит его от своего имени с кнопкой "📝 Создать задачу"
4. **Нажмите кнопку** "📝 Создать задачу" - кнопка изменится на "✅ Закрыть задачу"
5. **Закреплённое сообщение** автоматически обновится с новой статистикой
6. **Кликните на задачу** в закреплённом сообщении - вы перейдёте к сообщению с кнопками
7. **Нажмите** "✅ Закрыть задачу" для завершения задачи

### Пример закреплённого сообщения:

```
📋 Статистика задач

🔴 Открыто: 3 | ✅ Закрыто: 5

🧾 Открытые задачи:

1. Исправить баг в модуле X - @user1
2. Добавить новую функцию - @user2
3. Проверить документацию - @user3
```

Каждая задача - это **кликабельная ссылка** на сообщение с кнопками!

### Команды:

- `/start` - Приветственное сообщение
- `/refresh` - Пересоздать закрепленное сообщение (если вы удалили старое)

## Структура проекта 📂

```
task.pin.bot/
├── bot.py              # Основной файл бота
├── migrate_db.py       # Скрипт миграции базы данных
├── .env                # Токен бота (не коммитится в Git)
├── .gitignore          # Список игнорируемых файлов
├── requirements.txt    # Зависимости Python
├── start_bot.bat       # Launcher для Windows
├── tasks.db            # База данных SQLite (создаётся автоматически)
├── QUICK_START.md      # Краткая инструкция по запуску
└── README.md           # Документация
```

## База данных 💾

SQLite база данных `tasks.db` содержит две таблицы:

### Таблица `tasks`
- `id` - ID задачи (автоинкремент)
- `chat_id` - ID чата
- `user_id` - ID пользователя
- `username` - Имя пользователя
- `text` - Текст задачи
- `status` - Статус ('open' или 'closed')
- `created_at` - Время создания
- `message_id` - ID сообщения с кнопками (для создания ссылок)

### Таблица `chats`
- `chat_id` - ID чата (первичный ключ)
- `pin_message_id` - ID закреплённого сообщения

## Логирование 📊

Бот ведёт подробное логирование:
- ✅ Создание задач
- 🔒 Закрытие задач
- 📌 Обновление закреплённого сообщения
- ⚠️ Предупреждения и ошибки
- 🗑️ Удаление сообщений

## Требования ⚙️

- Python 3.8+
- aiogram 3.15.0
- python-dotenv 1.0.1

## Безопасность 🔒

- ✅ Токен хранится в `.env` файле (не в коде)
- ✅ `.env` добавлен в `.gitignore`
- ✅ База данных не коммитится в Git

## Остановка бота ⏹️

Нажмите `Ctrl+C` в терминале для корректной остановки бота.

## Возможные проблемы ⚠️

### Бот не удаляет сообщения
**Решение:** Убедитесь, что бот является администратором чата с правом "Удаление сообщений"

### Бот не закрепляет сообщения
**Решение:** Убедитесь, что бот является администратором чата с правом "Закрепление сообщений"

### Токен не найден
**Решение:** Проверьте наличие файла `.env` и правильность токена

## Автор

Разработано для управления задачами в Telegram чатах.

## Лицензия

MIT License
